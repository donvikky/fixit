package com.fixit.web.repository;

import com.fixit.web.entity.ProjectPhoto;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProjectPhotoRepository extends JpaRepository<ProjectPhoto, Integer> {
}
